# phpstorm project files
.idea

# netbeans project files
nbproject

# zend studio for eclipse project files
.buildpath
.project
.settings

# windows thumbnail cache
Thumbs.db

# composer itself is not needed
composer.phar

# Mac DS_Store Files
.DS_Store

# phpunit itself is not needed
phpunit.phar
# local phpunit config
/phpunit.xml

# sass files
.sass-cache
config.rb

node_modules/
# instalar gulp en local (node -v, npm -v y gulp-v han de devolver versión)
## npm init (project root)
## npm install gulp --save-dev

